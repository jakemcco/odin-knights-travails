//Handles calculations for how we want to move the knight

class Agent {
    constructor(){

    }

    calcKnightMove(board, knight, goal){
        const moves = [];
        //Build out moves in order, add to array

        return [[x1, y1], [x2, y2], [x3, y3]...]
    }
}

export default Agent;